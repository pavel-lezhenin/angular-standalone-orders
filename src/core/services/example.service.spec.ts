import { TestBed } from '@angular/core/testing';

/**
 * Example test for a service
 * Replace with actual service tests
 */
describe('ExampleService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

  it('should be created', () => {
    expect(true).toBe(true);
  });
});
